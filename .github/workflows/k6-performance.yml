name: K6 Performance Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: # Permite execução manual

jobs:
  performance-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Start REST API Server
      run: |
        npm start &
        sleep 10
      env:
        NODE_ENV: production
        JWT_SECRET: starwars_secret
        REST_PORT: 3001

    - name: Wait for server to be ready
      run: |
        timeout 60 bash -c 'until curl -s -f http://localhost:3001/api-docs > /dev/null; do echo "Waiting for server..."; sleep 3; done'
        echo "Server is ready!"

    - name: Test server connectivity
      run: |
        curl -I http://localhost:3001/api-docs
        curl -s http://localhost:3001/api-docs | head -10

    - name: Setup K6
      uses: grafana/k6-action@v0.3.1
      with:
        filename: test/k6/starwars-characters.test.js
        flags: --env BASE_URL=http://localhost:3001 --verbose
      env:
        K6_BROWSER_ENABLED: false

    - name: Upload K6 HTML Report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: k6-performance-report
        path: test/k6/reports/
        retention-days: 30

    - name: Comment PR with results
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          const fs = require('fs');
          try {
            const summary = JSON.parse(fs.readFileSync('test/k6/reports/starwars-characters-summary.json', 'utf8'));
            const metrics = summary.metrics;
            
            const comment = `## 🚀 K6 Performance Test Results
            
            ### ⚡ Performance Metrics
            - **P95 Response Time**: ${Math.round(metrics.http_req_duration.values.p95)}ms
            - **Checks Passed**: ${Math.round(metrics.checks.values.rate * 100)}%
            - **Total Requests**: ${metrics.http_reqs.values.count}
            - **Failed Requests**: ${Math.round(metrics.http_req_failed.values.rate * 100)}%
            
            ### 📊 Custom Metrics
            - **Character List P95**: ${Math.round(metrics.character_list_duration?.values.p95 || 0)}ms
            - **Character Create P95**: ${Math.round(metrics.character_create_duration?.values.p95 || 0)}ms
            - **Character Get P95**: ${Math.round(metrics.character_get_duration?.values.p95 || 0)}ms
            
            ### 🎯 Thresholds
            ${metrics.http_req_duration.values.p95 < 3000 ? '✅' : '❌'} P95 < 3000ms
            ${metrics.checks.values.rate > 0.95 ? '✅' : '❌'} Checks > 95%
            
            [📈 View detailed HTML report in artifacts](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})
            `;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });
          } catch (error) {
            console.log('Could not read K6 summary file:', error.message);
          }